[project]
name = "milyvoice-asr"
version = "1.0.0"
description = "Provider-neutral ASR adapters for MilyVoice 3"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["mily_asr*"]
