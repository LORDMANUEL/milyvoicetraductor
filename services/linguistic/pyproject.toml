[project]
name = "milyvoice-linguistic"
version = "1.0.0"
description = "Deterministic text, terminology, context and quality guards for MilyVoice 3"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["mily_linguistic*"]
