# Runtime base de MilyVoice. Los adapters opcionales se importan únicamente
# cuando MilyCompute los selecciona; instalados en disco no consumen RAM activa.
fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
numpy>=2.2,<3
faster-whisper>=1.1,<2
huggingface-hub>=0.34,<2
sentencepiece>=0.2,<1
safetensors>=0.5,<1

# Calidad / compatibilidad histórica. Son imports diferidos.
transformers>=4.51,<6
torch>=2.7,<3

# Engine Hub 2.1: runtimes alternativos locales para Windows/Python 3.13.
moonshine-voice==0.1.3
sherpa-onnx==1.13.6

# Audio del sistema Windows.
PyAudioWPatch==0.2.12.8; sys_platform == "win32"
